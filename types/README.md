# MoonbitHTTP/types

Protocol-independent HTTP types: validated lowercase header names, byte-valued
multi-map headers, generic requests/responses, methods, URIs, versions and
resource limits. This package does not depend on a transport or wire protocol.
